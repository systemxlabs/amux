# AGENTS.md

## 文档管理

- [产品需求](docs/PRD.md)和[系统设计](docs/DESIGN.md)文档主要描述整个框架，部分细节没有描述
- **产品需求、系统设计和本文档禁止改动**：如需改动，可向用户提出建议，由用户进行改动，文档提交和推送需用户显式同意
- 主动判断文档是否需要更新和完善——发现未覆盖、表述不清或已过时的决策时，提出修订建议

## 代码实现

- 代码实现必须遵循文档，文档未规定的部分可自行决策
- 代码无法实现文档要求时，不要擅自偏离文档，交由人来决策
- 代码实现允许随意破坏性改动，无需向后兼容，不为旧数据、旧格式、旧行为写迁移或兜底
- 代码实现尽可能复用已有的库，不要重复造轮子
- 代码实现遵循 Rust 和软件工程最佳实践
- 代码实现尽量使用强类型
- 鼓励更主动更激进地重构代码
- 主动清理死代码
- 允许直接提交并推送代码至主分支
- 代码注释应解释非显而易见的原理、不变量、安全约束或外部系统的怪癖，而不是复述代码本身

## 依赖管理

- 尽量依赖纯 Rust 库
- 尽可能保持依赖树精简，避免引入冗余传递依赖，主动清理过期依赖
- 主动更新依赖

## 测试

- 为真实可观察到的回归问题、非平凡的不变量或边界情况以及具体的 bug 添加测试。仅仅“代码被修改”或“覆盖率提升”本身不足以成为添加测试的理由
- 优先利用行为边界处的现有覆盖。避免编写仅复述字面量、映射关系、显而易见的控制流、实现细节或已移除特性的测试——除非其“缺失”本身就是一项契约。对于并发场景，在实际可行时，优先采用确定性的协调或受控调度，而非 sleep

## 仓库结构

| 组件 | 职责 |
|---|---|
| `amux-common` | 共享代码 |
| `amux-protocol` | 协议规范 |
| `amux-server` | Server 实现 |
| `amux-desktop` | 桌面应用 |
| `amux-web` | Web 应用（待定） |
